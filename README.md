# fish_clustering_visualization
This project involves utilising a fish big data dataset that will be used alongside four clustering algorithms. We will then assess to see which clustering algorithm is able to perform the group the fishes in a way that replicates the closest to the expected cluster groups. 
